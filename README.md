# make-value
## a bad attempt at making a value generator for Roblox

This is more a cute little project written in Rust that utilizes rbx-dom that uses a CLI to generate a value. (and also has no use at all)